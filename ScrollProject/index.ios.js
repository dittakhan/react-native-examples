/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 * @flow
 */

import {
  AppRegistry,
} from 'react-native';

import ScrollProject from './build/ScrollProject';

AppRegistry.registerComponent('ScrollProject', () => ScrollProject);
